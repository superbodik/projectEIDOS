# 🗺️ ROADMAP — EidosEngine v0.7

> Статус на 01.08.2026 | Активная версия: **v0.7 «Поверхность»**

---

## 📊 СТАТУС-БАР

```
v0.1 ████████████ v0.2 ████████████ v0.3 ████████████ v0.4 ████████████ v0.5 ████████████
v0.6 ████████████ v0.6.1 ████████████ v0.7 ████████░░░░ v0.8 ░░░░░░░░░░░░ v0.9 ░░░░░░░░░░░░
v1.0 ░░░░░░░░░░░░
```

---

## ✅ ФАЗА 0: КОРНИ (v0.1 – v0.5) — ГОТОВО

| Версия | Название | Ключевое |
|--------|----------|----------|
| v0.1 | Искра | Окно Raylib, статический мир |
| v0.2 | Порядок | Чанки 16×256×16, Perlin-шум |
| v0.3 | Свет | Освещение, день/ночь, небо |
| v0.4 | Тело | Игрок, физика, WASD, камера |
| v0.5 | Душа | Меню, инвентарь, Lua, консоль |

---

## ✅ ФАЗА 1: СТВОЛ (v0.6 – v0.7)

### ✅ v0.6 «Биом» — ГОТОВО
- 14 биомов (Ocean → Mountains)
- FastNoiseLite: FBm + Ridged + Domain Warping + Erosion
- Деревья: Oak, Spruce, Birch, Acacia, Jungle
- Руды: Coal, Copper, Iron, Gold, Diamond (Kimberlite)
- Автосохранение 60с, панорама меню, fog-шейдер

### ✅ v0.6.1 «Чистка» — ГОТОВО
- **18/25 багов** исправлено (список в БАГИ.md)
- **3-проходный рендер**: opaque + cutout(discard) + translucent(blend)
- **Прозрачность**: листва (дырки 20%), вода, стекло, цветы
- **Креативный инвентарь**: 65+ блоков, тултипы, перетаскивание
- **Пещеры v3**: входы с поверхности, соединённые, камеры, sinkholes, worm-туннели
- **3D рудные камни**: 6 типов на поверхности, состав в WAILA
- **Растительность по биомам**: зимние без цветов, пустыни — кактусы
- **Верхушки деревьев**: закрыты листвой
- **Цветы не над пещерами**: единый cave-чек для всей растительности
- **Прогресс-бар** загрузки мира (считает state≥3)
- **Фрустум-куллинг**: чанки за спиной не рендерятся
- **Оптимизация**: 16 чанков/кадр, IsAreaLoaded 3×3, upload limit 200

### 🟡 v0.7 «Поверхность» — В ПРОЦЕССЕ

#### ✅ Сделано:
- 🏞️ **Реки v2** — полная переделка генерации:
  - Долина вырезается в самом `GetHeight()`, а не пробивается в колонне —
    отсюда пологие берега вместо рваного жёлоба
  - Русло — нулевой уровень тёплого шума: сеть с притоками и развилками
  - **Извилистость**: domain warp смещает осевую линию
  - **Ширина в блоках**: поле делится на свой градиент (`|f|/|∇f|`)
  - **Flow-accumulation (прокси)**: река узкая в горах, широкая у моря;
    реки растворяются в океане и не лезут на пики
  - **Берега**: песчано-галечные отмели у уреза воды
  - Реки не режутся пещерами (иначе слились бы)
- 🌊 **Водопады v2**: уровень воды квантуется шагом 4 →
  цепочка плёсов со сливами
  - **Каскад, а не стена**: падает только середина русла
  - **Каменный уступ** (sill) с промоиной держит плёс сверху
  - **Озерцо внизу**: у подножия слива русло углубляется
- 💧 **Вода в реках**: заполнение русла, лёд в холодных биомах
- 🧪 **`tools/rivercheck.cpp`** — валидатор инвариантов воды
  (0 дефектов на 10 сидах, ~57 000 колонн русла)
- 🌊 **Рендер воды v2** (01.08.2026):
  - Отдельный анимированный атлас 16×512: 32 кадра, бесшовный по
    пространству и времени (`Chunk::LoadWaterAtlas`)
  - Вода в собственном меше со своим шейдером `water.vs/.fs`:
    покадровая анимация + дрейф двух слоёв, френель к небу,
    солнечный блик, туман — раньше вода шейдера не имела вообще
  - Волны по вершинам верхней грани, ватерлиния ниже кромки блока
  - **Под водой**: детект по реальному блоку у камеры (не по y<60!),
    синий туман 2–34 блока, экранная плёнка
  - **Ветер починен**: `vertexColor.a` нормализуется дважды →
    качание листвы/травы было мёртвым; высота качания теперь из UV
    тайла, а не из мировой Y (иначе деревья гнуло бы на 12 блоков)
- 🖼️ **Панорама починена**: зеркальные верх/низ куба, `rlSetTexture`
  вместо ломавшего батчинг `rlEnableTexture`, утечка текстур при
  перезагрузке, `UnloadPanorama` в деструкторе
- 🧭 **`locate biome` починен**: River был ненаходим (GetBiome его не
  возвращает), опечатка кидала в океан молча, радиус 8000 < климата
  (снега начинаются за ~18000). Теперь: 29 алиасов, `locate biome list`,
  радиус 32000, проверено 15/15 (`tools/biomecheck.cpp`)
- ⌨️ **Консоль v2**: история ↑/↓ в обе стороны с черновиком, навигация
  по подсказкам стрелками, клик и колесо мышью, Del/Home/End,
  автоповтор клавиш, вставка по курсору, поиск по подстроке
- 🕳️ **Пещеры v4** (01.08.2026 20:45):
  - Убраны гигантские «кратеры»-скрейпы у поверхности (баг со скринов)
  - Камеры-полости только глубже y=34 — река больше не висит над бездной
  - Тоннели расширяются с глубиной; общий `IsCaveAt` для генерации
    и декораций (раньше две копии шума расходились)
  - Страж воды: под руслом/океаном пещеры не режутся (24/12 блоков)
- 🌲 **Деревья: фикс якоря**: ствол и крона привязаны к высоте СВОЕЙ
  колонки — деревья больше не свисают листвой в реку и не плавают
  у обрывов; в русле и под водой деревья не растут
- 🎨 **Атлас v2**: все тайлы на детерминированном бесшовном value-noise
  (было: равномерный рандом-шум); кластерные жилы руд, волнистые слои
  осадочных, рваная кромка травы, дырки листвы пятнами, лава с жаром;
  стекло и лёд стали по-настоящему полупрозрачными
- ☀️ **Шейдеры v2**: направленное солнце + холодный ambient, ночью
  темнеет (раньше блоки ночью светились как днём), экспоненциальный
  туман; нормали добавлены растениям/камешкам (буфер расходился!)
- 🌊 **Вода v2.1**: world-space UV (муар от потайлового повтора убран),
  межкадровое смешивание анимации, блик гейтится гребнями волн
- 🌬️ **Ветер v2**: волны воды и качание растений масштабируются силой
  ветра; команда `wind <0-30>` / `wind auto`
- 🌱 **ЖИВОЙ МИР v1** (`src/Core/WorldTicks.cpp`):
  - Random-tick симуляция вокруг игрока (без участия игрока)
  - Трава: расползается на землю, гибнет под блоками
  - Высокая трава: разрастается и гниёт; цветы вянут
  - Дуб роняет жёлудь → **саженец** (новый блок OakSapling) → вырастает
    в дерево; листва без ствола рядом опадает
- 🍀 **Растительность: поляны**: плотность травы/цветов ведёт
  низкочастотный шум — луга и прогалины вместо равномерной сыпи
- ✨ **Небо v2 / «шейдерный» вид** (01.08.2026 20:57):
  - Солнце с процедурными лучами (билборд, аддитив) — терраны его
    заслоняют, сквозь дырки листвы просвечивают лучи
  - Экранный блум вокруг солнца, гаснет при отвороте камеры
  - Облака из 4 клубов вместо одной плиты, двухцветные, мягче
  - Пылинки в воздухе: 70 частиц, дрейфуют по ветру, мерцают
  - **Шов анимации воды убран** — выборка привязана к центрам текселов
  - **Листва ажурнее**: дырки 36% + дизеринг 10% — просвечивает
  - **Деревья не рубятся на границах биомов** — вид дерева решает
    колонка самого дерева, а не соседа
- 🌗 **День/ночь: инверсия исправлена** (01.08.2026 21:10):
  солнце стояло в `sin(t+π/2)` — под горизонтом в полдень; днём было
  темно с луной, «ночью» светило солнце, облака «жили только ночью».
  Исправлена фаза светил; луна получила холодное свечение-гало
- 🌍 **Полярный климат**: масштаб широты 20000 → 7000, вес широты 0.85 —
  экватор жаркий, полюса снежные; тайга в ~3800, снега в ~6000 от экватора
- 🌿 **Плавные переходы биомов**: джиттер температуры/влажности/маски гор
  на колонку — границы биомов дизерятся полосой смешения (экотоны),
  берега рваные, у подножий гор каменистая осыпь
- 🧊 **Текстуры льда**: трещины через ridge-шум, три вида (лёд, матовый,
  паковый), настоящая полупрозрачность
- 🍃 **Шов на листве убран**: между двумя листовыми блоками рисовались
  два встречных квада в одной плоскости (z-fight); теперь одна грань
- 💧 **Тёмные «ямы» в воде**: глубокие плёсы наследовали чёрный
  запечённый свет — поднят пол яркости воды
- 🏞️ **Реки v2.1 — плавные каскады** (01.08.2026 21:45):
  шаг плёсов 4 → 2 блока (перепады вдвое мягче, каскады чаще),
  долина шире и положе; промоина уступа жёстко ≤2 блоков, струя
  всегда шире промоины — 0 дефектов воды на 12 сидах
- 📜 **ПРОГРЕССИЯ.md** (01.08.2026 22:15): дизайн-документ всех 10 эпох
  (TFC/Vintage Story-механики + арка автоматизации: игрок руками →
  механизмы → программируемые системы). Каждой эпохе — механики,
  цепочка квестов, что автоматизируется
- 🌲 **ДРЕВО КВЕСТОВ (клавиша L)** (01.08.2026 22:15):
  - `src/Progression/QuestSystem` — 18 квестов Эпохи 1 на текущем
    контенте: добыча, биомы, глубина/высота, саженец, кимберлит
  - Последовательная разблокировка, автозачёт условий, счётчик
    прогресса, тост при выполнении
  - Эпохи 2–10 видны как анонс с перечнем ключевых квестов
  - Прогресс сохраняется в мире (`quests.txt`)
  - **Добыча в выживании починена**: сломанный блок падает в инвентарь
    (раньше просто исчезал — выживание не работало вовсе)
- 🧱 **Атлас 8×8 → 16×16** (блокер снят): 192 свободных слота под
  будущие блоки
- 🪵 **Новые блоки**: доски (Oak Planks), папоротник (Fern, влажные
  леса), камыш (Reed, берега рек и пляжи) — `give @s planks/fern/reed`
- 🎨 **Текстуры v3 — «взрослая» палитра** (02.08.2026 01:05):
  - Ушли мультяшные цвета: трава 55,155,50 → 88,110,52 (оливковая),
    вся палитра приглушена и затемнена, убраны чистые GREEN/RED/GOLD
  - Рендер тайла: тональное смешение по трём стопам (тень→база→свет)
    вместо «база + шум», плюс попиксельный сдвиг оттенка
  - 4 октавы шума вместо 3 — детализация без «грязи»
  - **Дерево**: непрерывное волокно (варпнутая синусоида) + сучки
    вместо жёстких двухтоновых полос; годовые кольца мягкие
  - **Руда**: жилы с тёмной каймой и светлым ядром — минеральные
    вкрапления вместо конфетти
  - **Лава**: корка → свечение → ядро
  - Берёза, дёрн, растения, вода — все переведены на смешение
  - **Починено**: тайлы Silt и Peat никогда не рисовались (белые
    квадраты в игре с самого начала)
  - **Починено**: снег был полупрозрачным льдом (регресс прошлой
    итерации — тайл 4,2 это Snow, а не лёд)
  - Папоротник и камыш перерисованы (были «гусеница» и три палки)
- ⌨️ **Клавиши** (02.08.2026 01:15): `~` — только консоль (раньше она
  открывалась и на F1), **F1 — скрыть HUD**: счётчик FPS, ветер, хотбар,
  прицел, WAILA, тост квестов, дебаг-оверлей и рамка выделения блока.
  Открытые инвентарь/древо квестов и консоль под F1 не гаснут
- 📖 **ПРОГРЕССИЯ.md переписан** (02.08.2026 01:40): эпохи получили имена
  и характер (Первичный Хаос, Кинематика, Термодинамика, Абсолют EIDOS).
  Добавлено:
  - **Смысл игры**: главный парадокс (строишь машины ради свободы от
    труда — а кто ты, когда труда нет), эмоциональная арка по эпохам,
    5 столпов дизайна, ставки и цена ошибки
  - **Прогрессия сервера** — раздел, которого не было: одиночная
    прогрессия на сервере превращает его в 50 параллельных игр.
    Решение — специализация по биомам и общие мегапроекты
  - Новые механики из хардкор-канона: обвалы шахт с креплением,
    валка дерева целым стволом, температурный контроль, порча еды
  - Версии эпох синхронизированы с ROADMAP (I=v0.9 … X=v2.5),
    мультиплеер выделен в v1.0.5
  - Названия эпох обновлены и в игре (древо квестов = «Нейросеть EIDOS»)

- 🔥 **ФАКЕЛЫ И БЛОЧНЫЙ СВЕТ** (02.08.2026 02:00) — разблокировали
  половину Эпохи I: 10 из 18 квестов ведут под землю, а источников
  света в игре не было вообще
  - Второй канал света: небо и блочный свет упакованы в полубайты
    одного байта (память не выросла), два независимых BFS-разлива
  - Свет факела **тёплый**: где блочный свет перебивает небесный,
    вершины краснеют — пещеры оранжевые, поверхность холодная
  - Факел: тонкая 3D-модель со свечением, уровень 14; лава светит 12
  - `give @s torch`
- 🎒 **Инвентарь на настоящих текстурах**: слоты, хотбар, креативная
  сетка и перетаскиваемый предмет рисуют тайл из атласа вместо плоской
  заливки цветом; вода берёт кадр из своего атласа
- 🪨 **Камешки v2**: 8 новых типов — кремень (нужен для кнаппинга
  Эпохи I), гранит, базальт, известняк, песчаник, олово, серебро, цинк.
  Порода зависит от биома, руда — от глубинного хеша
  - Модель заменена с коробки на гранёный валун: 6 граней, случайный
    поворот, размер и смещение от координат — два камня рядом разные
- 🌿 **Растения перерисованы**: трава — веер из 7 изогнутых травинок
  (была палка с кляксой), цветы — стебель с листьями и лепестками
  вокруг сердцевины, грибы — купол с ножкой и тёмным исподом,
  сухой куст — голые ветки. Красный гриб получил свой тайл
  (делил тайл с розой)
- 🖼️ **Панорама меню процедурная**: 6 граней кубмапы генерируются
  из направления взгляда — слоистые хребты со снежными шапками,
  рассветное свечение, звёзды, дымка. Файлы на диске больше не нужны,
  `makepanorama` и F9 удалены
- 🌡️ **РЕАЛИСТИЧНАЯ ТЕМПЕРАТУРА** (02.08.2026 02:07) —
  `WorldGenerator::GetClimate`, температура в °C из четырёх слагаемых:
  - **Широта**: +38 °C на экваторе → −30 °C у полюсов
  - **Высота**: −0.34 °C на блок над уровнем моря (горы на y=120
    теряют больше 20 °C — снег там лежит не «по биому», а по холоду)
  - **Сутки**: амплитуда зависит от влажности — пустыня качается
    на ±15 °C, влажный лес на ±4.5; пик в 13:00, а не в полдень
  - **Глубина**: ниже 5 блоков от поверхности температура сходится
    к пещерным 11 °C; у бедрока добавляется геотермальный нагрев
  - **Ощущаемая**: факелы и лава греют (+16 °C у огня), ветер студит
    на холоде, вода отнимает 9 °C
  - Температура тела игрока с инерцией, статусы COLD / HOT
- 🎒 **Инвентарь v2 — три вкладки**: ITEMS / NUTRITION / ENVIRONMENT
  - Панель перерисована: тёмный фон, рамка, подсветка активной вкладки
  - **ENVIRONMENT**: воздух и тело в °C с цветовыми шкалами, биом,
    высота, влажность, ветер, время, уровни неба и блочного света,
    под землёй или нет, и разбор температуры по слагаемым
  - **NUTRITION**: сытость + 4 группы (злаки/овощи/фрукты/белок).
    Список съедобного с эффектами появился ниже — см. «Еда и
    собирательство»
- 🩸 **РЕЖИМ ВЫЖИВАНИЯ v1** (02.08.2026 02:20) — `src/Core/Survival.cpp`:
  - **Здоровье, голод, жажда** — реальные шкалы, а не декор
  - Голод и жажда тратятся быстрее при беге и температурном стрессе;
    жажда в жару растёт почти вдвое
  - **Вода восполняется купанием** в реке или озере
  - Бег требует запаса сытости; на голодный желудок скорость падает
  - **Урон**: от голода, от обезвоживания (вдвое быстрее), от
    переохлаждения ниже 33 °C и перегрева выше 39.5 °C
  - **Регенерация упирается в питание**: потолок здоровья зависит от
    худшей группы нутриентов — на одной каше выше 35 % не подняться
  - Смерть → респавн на поверхности с потерями
  - Всё это только в Survival; в Creative шкалы заморожены
- ❤️ **HUD выживания в стиле Minecraft** (02.08.2026 02:30):
  ряды пиксельных иконок вместо полосок — **10 сердец слева**,
  **10 кусков еды и 10 капель воды справа** от хотбара, с половинками
  и тёмной подложкой. Слева термометр с ртутью и цифрой температуры
  тела, пульсирующее FREEZING / OVERHEATING по центру.
  **Выносливость убрана** — вместо отдельной шкалы бег ограничен
  сытостью, как в Minecraft
- 📐 **HUD масштабируется под экран**: `OverlayUI::ComputeLayout` —
  целочисленный GUI-масштаб 1–4 по меньшей стороне экрана (как в
  Minecraft), от него считаются размер слота, хотбар, иконки и шрифты.
  Хотбар и HUD берут геометрию из одного источника, поэтому не
  разъезжаются. **Починен наложение**: при 1080p ряд иконок был 237 px
  при половине хотбара 200 px — сердца налезали на еду в центре;
  теперь шаг иконок привязан к ширине хотбара
- 🐛 **Починено по скриншоту**: тело стартовало с 20 °C (игрок рождался
  переохлаждённым и «мёрз» в пустыне при +36); шкала тела считалась по
  формуле, дававшей >1 и всегда упиравшейся в максимум; текст вкладки
  ENVIRONMENT вылезал за рамку панели — высота панели теперь зависит
  от вкладки. Добавлена строка «Feels like» (с учётом огня, ветра, воды)

- 🪟 **Окно и меню** (02.08.2026 03:10):
  - `FLAG_WINDOW_RESIZABLE` + F11 через `ToggleBorderlessWindowed`,
    квадратик разворота и растягивание мышью работают; минимальный
    размер 854×480, размер окна сохраняется в конфиге
  - Меню переписаны на единую дизайн-систему (`Button`, `Toggle`,
    `Slider`, `TextField`, `DrawPanel`, виньетка): настройки теперь
    доступны **из паузы**, добавлена вкладка «Управление», удаление
    мира с подтверждением, `MenuPanorama` скрыт из списка миров
- ⚡ **ОПТИМИЗАЦИЯ.md** (02.08.2026 03:30): реестр всех оптимизаций —
  что уже есть (кеш колонн ~250×, ранний выход рек, maxY, отсечение
  граней, фрустум, упакованный свет), что дальше по приоритету
  (индексный буфер −33%, **секции чанка 16³** — главная структурная
  правка, greedy meshing 5–10×). Отдельно зафиксировано: **профайлинга
  в проекте нет**, правило «не оптимизировать без замера»
- 🔐 **АККАУНТЫ** (03.08.2026, `public/site/auth.py`): регистрация и вход
  на сайте и **в лаунчере одним аккаунтом**. PBKDF2-HMAC-SHA256,
  210 000 итераций, соль на пользователя, сессии по токену
  (кука для сайта, Bearer для лаунчера), лимит 8 попыток входа за 15 мин.
  Ник из аккаунта показывается над персонажем в игре
- 📚 **ВИКИ НА САЙТЕ** (03.08.2026): `/wiki` и `/updates`, EN/UA.
  Генерируется **из самих исходников движка** (`wiki_data.py` парсит
  BlockInfo, FoodSystem, QuestSystem, WorldGenerator, ROADMAP), поэтому
  не может разойтись со сборкой. **104 блока показаны настоящими
  тайлами**: `tools/atlasdump.cpp` выгружает атлас движка в
  `static/atlas.png` + карту «блок → тайл». Разделы: блоки и текстуры,
  геология по провинциям, где искать руду, еда, дерево квестов Эпохи I,
  биомы, управление, команды
- 📦 **Релизы и загрузка**: `tools/make_release.py` пакует сборку в zip
  с sha256 и заметками из ROADMAP; `/api/releases` отдаёт манифест,
  `/download/<файл>` — сам файл. **Скачивание сейчас закрыто**
  (`DOWNLOADS_ENABLED=0`): аккаунт регистрируется и место бронируется,
  но кнопки загрузки нет ни на сайте, ни в лаунчере
- 🖥️ **Лаунчер v2 + .exe**: вход/регистрация прямо в лаунчере,
  скачивание с прогрессом, проверка sha256, распаковка в `games/<версия>`,
  запуск игры с передачей ника. `build_exe.py` собирает
  `CRC-EIDOS-Launcher.exe` (PyInstaller, 20 МБ, своя иконка)
- 📨 **Почта**: подключён Resend (ключ в `public/site/.env`, файл закрыт
  в .gitignore), добавлен запасной путь через SMTP. Проверено живьём —
  Resend принимает письма (200)
- 🌐 **Сайт и лаунчер** (`public/`, 02.08.2026 03:50):
  - `public/site` — FastAPI + SQLite: заявки в бету, EN/UA, галерея
    собирается сама из `static/shots/`, rate-limit 5/час на IP, дедуп
    с повторной отправкой письма, работает и без ключа Resend
  - `public/launcher` — лаунчер: находит сборку, тянет версию и список
    изменений прямо из ROADMAP.md, запускает игру, считает наигранное
    время, показывает миры из `saves/`
  - Проверено вживую: EN/UA отдаются 200, заявка 201, повтор 200
- 🪨 **ГЕОЛОГИЯ v1 — связная модель недр** (02.08.2026 04:20).
  Было: порода выбиралась хешем на клетку 16×16 — крапчатая мозаика,
  и **12 из 22 пород не генерировались вообще**. Стало:
  - **5 геологических провинций** (Щит, Вулканическая, Осадочный
    бассейн, Карбонатная платформа, Складчатый пояс) — ячейки Вороного
    с доменным варпом, средний размер **727 блоков**
  - **Пласты**: у каждой провинции своя колонка залегания, границы
    волнистые (складки) — в обрыве видно слои, а не рябь
  - **Руда привязана к вмещающей породе**, а не к глубине вообще:
    кимберлит только в щите (100%), малахит в вулканической (100%),
    галенит и сфалерит в карбонатах (MVT), касситерит в граните,
    золото в метаморфических жилах
  - **Жилы**: сквозные тела с утроенной концентрацией руды
  - **Галька читается как признак недр**: тип камешка = порода под
    ногами, рудный камешек = руда, которая тут реально есть.
    Обобщённой «Stone Pebble» стало 30% → **2.4%**
  - Дорисован тайл Claystone — последняя порода без текстуры
  - `tools/geocheck.cpp`: 22/22 пород достижимы, 4.9 смены породы
    на колонку (пласты, не шум), 0 расхождений гальки с породой
- 🍎 **ЕДА И СОБИРАТЕЛЬСТВО** (02.08.2026 04:40) — вкладка NUTRITION
  перестала быть пустой. **Починен разрыв в выживании**: нутриенты
  только убывали, восполнить их было нечем, поэтому потолок
  регенерации здоровья необратимо падал до 35%
  - `src/Inventory/FoodSystem` — таблица еды; **правый клик = съесть**
  - **Ягодный куст** (новый блок, 2 стадии): растёт в лесах и тайге
    пятнами, спеет и расползается в тиках мира; сбор оставляет куст
  - Собирательство с разрушения: жёлудь и **птичье гнездо** из листвы,
    личинки из-под коры брёвен, волокно из травы
  - Все четыре группы закрыты сборкой Эпохи I: ягоды (фрукты),
    жёлудь (злаки), личинки и яйцо (белок), бурый гриб (овощи)
  - **Красный гриб ядовит** — мир учит читать себя
  - `tools/foodcheck.cpp` поймал: треть кустов ставилась на камень и
    песок и сгнила бы на первом тике; теперь только на почве
- 🌲 **Древо квестов 18 → 23**: геология сломала два квеста —
  «Stonecutter» требовал блок Stone, который больше не генерируется,
  а «Reading the Land» ждал обобщённую гальку (её стало 2.4%).
  Добавлен тип квеста «любой из списка» и «N разных» — фиксированный
  ID породы неверен в принципе, ведь порода зависит от провинции.
  Новая ветка: волокно → первая еда → сбалансированный рацион.
  Описания руд теперь учат геологии (медь в вулканической,
  золото в жилах пояса, кимберлит только под щитом)

- 👤 **ИГРОК: МОДЕЛЬ, РУКИ, НИК** (03.08.2026 21:40):
  - `src/Entities/PlayerModel` — блочный персонаж: голова, тело, руки,
    ноги, с махом конечностей от скорости и наклоном головы по взгляду
  - **F5 — вид от третьего лица** (сзади / спереди / от первого)
  - **Руки от первого лица**: предплечья с рукавами по нагруднику,
    предмет в руке рисуется настоящим тайлом из атласа, замах по клику
  - **Ник над персонажем** берётся из аккаунта: лаунчер передаёт
    `EIDOS_PLAYER`, движок подписывает игрока в мире
  - Слоты брони (голова/торс/ноги/обувь) и **косметики** (шляпа, плащ,
    подвеска, аура) — надетое видно и на модели, и в инвентаре
- 🎒 **ИНВЕНТАРЬ v3** (03.08.2026 21:40): вкладки переехали **вправо и
  стали плитками** с иконкой блока и подсказкой при наведении; слева —
  **кукла персонажа** с ником и колонкой слотов экипировки;
  новая вкладка **COSMETICS**. Предмет кладётся в слот перетаскиванием
- 📸 **Режим автоснимков** (`EIDOS_UISHOT=1`) — 8 кадров интерфейса и
  видов игрока пишутся в `ui_shots/`. Нужен был, чтобы **видеть** UI,
  а не чинить вслепую: он же и нашёл четыре бага ниже
- 🐛 **Найдено автоснимками и починено**:
  - Портрет игрока в инвентаре стирал всю панель: `BeginTextureMode`
    внутри `BeginDrawing` рвёт последующую 2D-отрисовку. Кукла
    переведена на плоскую 2D-графику
  - Скриншот снимался **до сброса батча raylib**, поэтому весь 2D-UI
    не попадал в кадр (на экране он был)
  - Руки были в полоску: неотекстурированные квады наследовали атлас
    и старые UV — теперь привязана белая текстура движка
  - **`time set day` уводил в полночь**: `SkySystem::timeOfDay` — доля
    0..1, а команда передавала часы (8.0 → 8 полных оборотов ≡ 0).
    Теперь принимаются и имена, и часы 0–24
- ⏩ **`gamerule randomTickSpeed <n>`** как в Minecraft: ускоряет или
  замораживает рост, созревание и распад. 0 останавливает мир,
  3 — обычная скорость
- 🎬 **Режим трейлера** (`EIDOS_TRAILER=1`) — семь кинематографичных
  проходов камеры по найденным биомам с титрами, кадры пишутся в
  `trailer_frames/` для сборки в видео

- 🧰 **КРЕАТИВ ПЕРЕПИСАН** (03.08.2026 22:50) — `src/Inventory/CreativeCatalog`:
  каталог **строится сам** из списка блоков, поэтому любой новый блок попадает
  в креатив автоматически. Было 78 блоков хардкодом (мимо каталога проходили
  Claystone, Conglomerate, Dolomite, Chert, ягоды, еда, часть камешков) —
  **стало 104**, разложенных по группам: жидкости, грунт, осадочные,
  магматические, метаморфические, руды, дерево, растения, камешки, еда.
  Добавлены заголовки групп, прокрутка с полосой, подсказка с названием и id
- 🎥 **Трейлер: инструмент сборки** — `tools/make_trailer.py` рендерит кадры
  в 1080p через режим `EIDOS_TRAILER` и кодирует их ffmpeg в mp4 (H.264,
  CRF 18, faststart — готово для YouTube) плюс webm и постер для сайта
- 🛰️ **API_И_ЛАУНЧЕР.md** — проектный документ: что в API есть сейчас, лента
  обновлений, скины и косметика, серверы, отчёты о падениях, флаги функций,
  порядок работ и требования безопасности
- 📰 **ЛЕНТА ОБНОВЛЕНИЙ В API** (`updates_store.py`): таблица `updates`,
  `GET /api/updates`, `/latest`, `/<версия>`, публикация через
  `POST /api/admin/updates` под `ADMIN_TOKEN`.
  `tools/publish_update.py` собирает запись из ROADMAP.md и постит одной
  командой — added / fixed / changed раскладываются автоматически.
  Проверено: публикация 201, чтение обратно, без токена 401
- 🐛 **Починено по скриншотам** (03.08.2026 22:50):
  - **В виде от третьего лица (F5) нельзя было двигаться**: направление
    движения бралось как `camera.target − camera.position`, а когда камера
    уходила выше игрока, этот вектор после обнуления Y становился нулевым —
    нормализация давала мусор. Теперь направление считается от глаз игрока
  - Камера третьего лица уезжала в вид сверху: угол наклона теперь
    ограничен −35°…+45°, камера всегда за спиной
  - Луч взаимодействия шёл от камеры, а не от глаз — в третьем лице ломалась
    добыча блоков
  - Вкладка NUTRITION вылезала списком еды за границу панели
  - Кукла не влезала в свой бокс: размер теперь считается по обеим сторонам,
    фигура центрируется и обрезается по рамке
  - Убрано вращение куклы мышью

- ⚖️ **БАЛАНС.md** (03.08.2026 23:55) — весь баланс в одном месте, переписан
  в **хардкорную** сторону:
  - **Три режима + свой**: Казуал / Выживание (по умолчанию) / Хардкор, плюс
    независимая галочка «Одна жизнь». Таблица различий по 15 параметрам
  - **Настройки при создании мира**: длина суток, расход шкал, распад нутриентов,
    зона комфорта, штраф за смерть, плотность руды/пещер/рек, масштаб широты,
    уровень моря, скорость тактов. Правило интерфейса — каждая настройка
    показывает, **что она ломает**
  - **Ступени истощения**: бег → скорость → дрожь → урон. Смерть от голода 71 с,
    от жажды 36 с — но с тремя предупреждениями до неё
  - **Смерть роняет вещи** (лежат 20 игровых минут), в хардкоре ещё −20 %
    максимального здоровья на сутки
  - **Мультиплеер спроектирован**: авторитетный сервер, вход тем же аккаунтом
    через `POST /api/auth/verify`, шкалы выживания считаются только на сервере,
    **эпоха общая на весь мир, квесты личные**, специализация по биомам,
    заявки на землю 32×32 с истечением, серверный античит, цель 20–40 игроков
- 👤 **3D-персонаж в инвентаре** (03.08.2026 23:40) — модель рендерится в текстуру
  **до `BeginDrawing`**: `BeginTextureMode` внутри кадра рвёт всю последующую
  2D-отрисовку, из-за этого панель раньше пропадала целиком
- 🧊 **Предметы в руке**: некубические (факел, растения, еда, камешки) рисуются
  послойной экструзией спрайта в 9 слоёв — объём как в Minecraft, только толще.
  Раньше факел был полным кубом с натянутой текстурой
- 📐 **Инвентарь масштабируется** от `min(ш/1280, в/720)`, зажат 0.62–2.2: слоты,
  отступы, шрифты, панель персонажа и полоса вкладок из одного коэффициента.
  Иконки блоков стали изометрическими кубами везде
- 🎥 **F5 больше не дёргается**: позиция камеры сглаживается экспоненциально
  по времени кадра, при смене вида сглаживание сбрасывается

- 🧱 **ВИСЯЩИЕ БЛОКИ** (04.08.2026 00:30) — `tools/floatcheck.cpp` просканировал
  **51 млн блоков** на площади 1800×1800:
  - **Висящей воды нет вообще** (0), сухого русла нет, боковых утечек нет
  - «Вода с открытым боком» — 2088 случаев — оказалась **не багом**: это кромки
    сливов. Уровни плёсов квантуются шагом 2 блока, у водопада вода обязана
    иметь открытую грань. Проверка научена их отличать → 0 ложных срабатываний
  - **4 настоящих висящих блока** (3 дёрна, 1 кварцит) — поверхность над
    полностью выеденной пещерой. Теперь если блок под поверхностным вырезан
    пещерой, вырезается и он: получается вход в пещеру вместо парящего блока
  - После правки: **0 висящих блоков, 0 висящей воды, 0 стенок воды**.
    Регрессия проверена — rivercheck, geocheck, foodcheck чистые

- 💀 **ХАРДКОРНЫЙ БАЛАНС В КОДЕ** (04.08.2026 01:10) — `src/Core/WorldRules`:
  - **Три режима на экране создания мира**: Казуал / Выживание / Хардкор,
    с описанием под кнопками, что каждый меняет
  - Правила применяются реально: длина суток, расход сытости (1.15/сут),
    жажды (1.85/сут) и нутриентов (0.70/сут), множитель урона, пороги холода
    и жары, потолок регенерации, штраф за смерть, «одна жизнь»
  - **Ступени истощения вместо голого таймера**: бег отключается при сытости
    <0.45, скорость падает на 15% при <0.25 и ещё на 18% при <0.10, и только
    потом урон. Смерть от голода 71 с, от жажды 36 с — с предупреждениями
  - **Смерть роняет вещи** (режим Выживание и выше), в Хардкоре ещё −20%
    максимального здоровья, которое восстанавливается за игровые сутки
  - Правила пишутся в `level.dat`; старые миры без строки `rules` открываются
    в режиме «Выживание», формат версионирован
  - **Настройки генерации подключены и проверены** (`tools/rulescheck.cpp`):
    `oreDensity` ×3 поднимает руду 1.45 % → 4.27 %, `caveDensity` 0 полностью
    убирает пещеры, `latitudeScale` 1500 сжимает климат (0.93 → 0.08 за 1400
    блоков). **При значениях по умолчанию мир побитово тот же** — 42 672 блока
    сравнения, 0 расхождений, старые сиды не поехали
  - ⚠️ `treeDensity` был подключён, но проверка показала **нулевой эффект** —
    ручка убрана, а не оставлена обманкой. `riverDensity` и `seaLevel` пока
    не подключены (`seaLevel` — `constexpr`, завязан на весь генератор)

- 🟠 **КУСОЧКИ МЕТАЛЛА — вход в Эпоху II** (04.08.2026 01:50):
  - Рудный камешек теперь даёт **1–3 самородка** вместо самого камешка:
    медь, олово, серебро, золото. Нарисованы 4 тайла — крупинки металла
    с бликом и тёмной каймой
  - Новый квест «Pieces of Copper»: собрать 12 самородков меди. В описании
    сразу заложена суть эпохи — **медь нельзя ковать, только лить**
  - Каталог креатива подхватил их сам: **104 → 108 блоков**, без правок
    в самом креативе
- 📖 **ПРОГРЕССИЯ.md: полный путь медной кирки** — цепочка из 10 шагов
  по образцу Vintage Story: кусочки с земли → глина → тигель и форма →
  ямный обжиг → **щипцы** (раскалённый тигель голыми руками не взять) →
  плавка при 1085 °C → заливка в форму → остывание своим временем →
  медная кирка. Кирка **не крафтится**, её отливают

- 🏺 **ГЛИНА И ГОНЧАРСТВО — второе звено Эпохи II** (04.08.2026 02:20):
  - **Глина копается в комки**: блок глины даёт 3–5 `Clay Lump` вместо себя
  - **Лепка на месте**: правый клик с комками в руке → **необожжённый тигель**
    (4 комка), с зажатым **CTRL** → **форма кирки** (3 комка). В сообщении
    сразу сказано главное: сырая глина в огне трескается, нужен обжиг
  - Добавлены предметы всей цепочки: комок глины, тигель необожжённый/обожжённый,
    форма необожжённая/обожжённая, **щипцы**. Нарисованы 6 тайлов —
    сосуд с сужением кверху, форма с тёмной полостью, деревянные щипцы
  - Два новых квеста: «Wet Clay» (8 комков) и «The Crucible» (слепить тигель),
    в описании второго — как именно лепить
  - Каталог креатива снова подхватил всё сам: **108 → 114 блоков**
  - ⚠️ Лепка пока прямая, без послойной мини-игры из ПРОГРЕССИЯ.md —
    мини-игра заменит её позже, механика уже работает

- 🌍 **БИОМЫ 15 → 28** (04.08.2026 03:10) — климат стал читаться подробнее:
  болото, мангры, бамбуковый лес, солончак, степь, чапараль, березняк,
  секвойи, вересковая пустошь, холодная пустыня, альпийский луг, ледник,
  вулканические пустоши
  - Каждому — своя поверхность (торф на пустоши, мел на солончаке, базальт
    на вулканических, паковый лёд на леднике) и своя растительность
  - Горы теперь ветвятся по температуре: **ледник** в холоде, **альпийский
    луг** в тепле и влаге
  - Низины отделены от возвышенностей: болото и мангры **только в низинах**
  - 29 новых алиасов для `locate biome` (bog, marsh, prairie, chaparral,
    sequoia, heath, icecap…)
  - **Проверено `biomecheck`: 28/28 биомов достижимы**, все имена совпадают
- 🐛 **Спавн был за полюсом** — в коде стояло `safeZ = 12000` при масштабе
  широты 7000, то есть каждый новый мир начинался на вечной снежной шапке.
  Теперь спавн ищет умеренный лес около `z ≈ 2600` с откатом на дождевой
  лес → саванну → тайгу и не ставит игрока в воду
- 🎨 **UI сведён к одной палитре** (`src/UI/UITheme.h`): в игре было **три**
  разных стиля — сине-серые меню, тёплый янтарный инвентарь и серые слоты
  из первой версии. Теперь всё берёт цвета из одного места
- 📐 **Панель инвентаря перестала прыгать**: её высота считалась отдельно
  на каждой вкладке, поэтому при переключении она раздувалась и сжималась.
  Размер зафиксирован по самой высокой вкладке, содержимое центрируется
- 🪨 **`Red Sandstone` был почти недостижим** (4 блока на 51 млн): требовал
  одновременно биом Scorched и осадочную провинцию. Теперь красный песчаник
  лежит у поверхности во всех выжженных землях и солончаках — 22/22 породы
  достижимы

- 📄 **ИДЕЯ.md** (04.08.2026) — питч проекта для инвесторов: проблема рынка,
  геология как технологический ров, арка автоматизации, что уже работает
  с цифрами проверок, бизнес-модель без pay-to-win, аудитория, дорожная карта
- 🌍 **БИОМЫ_ПЛАН.md** — список Biomes O' Plenty переведён в нашу шкалу
  (`temp = bop/1.2`, `hum = downfall/2.0`) и разбит на 5 партий. Правило:
  **биом без своих блоков не добавляем** — перекрашенная копия это не биом
- 🎨 **Текстуры: объём и тени** — в базовый painter добавлена лямбда `Relief`:
  направленный свет сверху-слева, затемнённая правая-нижняя кромка, подсветка
  выступов по шуму. Работает сразу на всех каменных, грунтовых и рудных тайлах;
  рудные вкрапления стали светлее вмещающей породы и читаются выпуклыми

- 🌊 **РЕКИ ДОХОДЯТ ДО МОРЯ** (04.08.2026) — русло существовало только там, где
  рельеф выше **63**, при уровне моря **60**. Река обрывалась за три блока до
  воды и просто исчезала, устья не смыкались с океаном. Порог опущен до
  `SEA_LEVEL - 1.5`. Проверено `tools/mouthcheck.cpp`: нижний уровень воды
  теперь **y=60**, и **100% устьевых колонн смыкаются с открытой водой**
- 🌱 **Растения только на почве** — растительность не проверяла, на чём стоит,
  и лезла на голый камень у воды. Теперь только дёрн, земля, грубая земля,
  грязь, торф и песок. `tools/placecheck.cpp`: **0 растений на камне** из 56 406
- ⛰️ **Горные биомы не тонут** — биом выбирался по маске гор без учёта высоты,
  и «гора» могла выпасть на низину у берега. Добавлено `heightBase > 0.52`:
  **0 из 11 207** горных колонн на уровне моря или ниже
- 🎨 **Текстуры: чистые цвета вместо шума** — четыре подхода я потратил на то,
  чтобы сделать их «сложнее» (шум, крапчатость, фаска), и получил грязь и
  **сетку из плиток**: фаска на каждом тайле размножалась по блокам и
  превращала землю в кафель. Убрано. Сейчас `Solid` даёт три тона по мягкому
  шуму, разброс яркости сужен с ±30% до ±13%, палитра вернулась к чистым
  читаемым цветам. Объём даёт затенение граней в меше, оно уже было

- 🧩 **Пять крупнейших файлов разложены на подмодули** (09.08.2026) —
  `Chunk.cpp` (2446 строк), `EidosEngine.cpp` (1649), `WorldGenerator.cpp`
  (1330), `Inventory.cpp` (853), `MenuSystem.cpp` (700). Механический
  перенос без изменения поведения — построчные срезы, а не ручной набор,
  каждая функция сверена, что встречается ровно один раз, полная
  регрессия по всем 8 валидаторам после каждого файла. Карта — в
  [МОДУЛИ.md](МОДУЛИ.md). Заодно поймана регрессия в самой вики: сайт
  парсит `WorldGenerator.cpp` регулярками для получения списка руд и
  биомов, а после разбиения файла этот путь исчез — руды и биомы
  молча обнулились. Не поймано валидаторами (они только про C++),
  поймано ручным прогоном `wikidump.py`

- ⛏️ **Инструменты добываемы в выживании** (09.08.2026) — раньше система
  добычи требовала кирку/топор, но получить их можно было только через
  креатив. Добавлена цепочка правым кликом по руке (без новой сетки
  крафта, по образцу уже существующей лепки глины): бревно → доски →
  палки; растительное волокно ×4 → верёвка; камешек ×3 (кремень ×2) →
  обтёсанный наконечник (модификатор клавиши выбирает какой: без —
  кирка, CTRL — топор, ALT — лопата, SHIFT — нож, CTRL+SHIFT — мотыга);
  наконечник + палка + верёвка → готовый инструмент. Цепочка не
  циклична: дерево рубится голыми руками медленно, но рубится — этого
  достаточно, чтобы дойти до первого инструмента без готовых инструментов

- 🖥️ **ЖИВОЙ ОТЧЁТ О ГЕНЕРАЦИИ В КОНСОЛЬ** (07.08.2026). Раз в полсекунды
  печатается, что происходит прямо сейчас: стадия, число чанков по
  состояниям (пустой / рельеф / меш готов / живой), длины обеих очередей,
  сколько занято и сколько залито на видеокарту за кадр

- 🔴 **НАЙДЕНА ПРИЧИНА ВЕЧНОЙ ЗАГРУЗКИ И ДОЛГОЙ ГЕНЕРАЦИИ** (07.08.2026).
  Отчёт в консоль показал то, чего не было видно на глаз:
  - `EidosEngine.cpp:720` — собрав меш, чанк помечает `dirty` **всех
    восьмерых соседей**. `UpdateChunks` возвращает каждого в очередь,
    тот пересобирается и снова метит своих восьмерых. **Цепная реакция**:
    очередь растёт быстрее, чем её разбирают семь потоков
  - Замер: **очередь мешей 874 при 1104 чанках, готовых мешей 0,
    занято 992 из 1104**. Потоки заняты почти целиком лишними пересборками
  - Загрузочный экран ждёт 25 чанков вокруг игрока в состоянии 3. Они
    стоят в конце очереди FIFO за тысячами лишних пересборок и не
    доходят — отсюда **вечные 0%** на экране загрузки
  - **ИСПРАВЛЕНО.** Гипотеза подтвердилась на неподвижном мире:
    свет чанка зависит от света соседей, соседи переписываются другими
    потоками, поэтому `memcmp` в `CalculateBasicSunlight` всегда видит
    расхождение и волна не гаснет. Добавлен счётчик `lightPasses`,
    ограничивающий распространение тремя проходами после реального
    изменения блоков. **Очередь падает в ноль и остаётся там**:
    361 живой чанк, `queue m=0`, `busy 0`, заливок на видеокарту нет.
    Раньше в этом же месте стояло ~330 в очереди и 10 пересборок в
    секунду вечно
  - ⚠️ Урок: первый мой замер объявил правку неудачной, потому что шёл
    в режиме тура по биомам с непрерывной телепортацией камеры — в нём
    очередь не успокаивается по определению. Проверять оседание
    конвейера можно только на неподвижном мире
  - **Что выяснено дополнительно и куда смотреть дальше:**
    `BuildMeshCPU` в конце **безусловно ставит `state = 2`**, даже если
    чанк уже был живым. Значит любая пересборка сбрасывает живой чанк
    обратно и требует повторной заливки на видеокарту. В логе видно
    7–70 заливок **каждый кадр без остановки** — это постоянный поток
    на GPU на ровном месте
- 🫐 **ЯГОДНЫЕ КУСТЫ ЗАМЕДЛЯЮТ ИГРОКА** (07.08.2026). `BerryBush`,
  `BerryBushRipe`, `CranberryBush` были физически непроходимы —
  `IsPassable` их не пропускал, значит и добавленное замедление никогда
  бы не сработало: коллизия выталкивала игрока раньше, чем он входил
  в клетку куста. Теперь они проходимы, а `UpdateSurvival` снижает
  скорость до 45% при ходьбе через них (листва — 35%, для сравнения).
  Действует только в Survival: полёт/Creative через `UpdateFlying`
  замедление не трогает, как и должно быть

- ⛏️ **Все руды из таблицы запасов добавлены, старые камешки починены** (07.08.2026).
  Проверка вскрыла: часть Эпохи I уже была реализована в
  более ранней сессии — самородки, глина, гончарка, яма для обжига,
  щипцы (`ClayLump`, `Crucible`, `PickMould`, `Tongs`, `FirePit` уже
  существовали), и у камешков **уже есть** своя объёмная геометрия
  (`AddPebbleFaces`), не блочная — то, что нужно было для скриншота
  с подписями Flint/Loose Stones, было готово
  - **Найден реальный баг в текстурах**: 8 из 14 существующих камешков
    либо указывали на **непрокрашенные прозрачные ячейки атласа**
    (кремень, олово, цинк были невидимы), либо **занимали чужую
    плитку** чужой жилы (медь показывала цвет сфалерита, железо —
    галенита). Хуже всего — **золото и серебро указывали на одну и
    ту же ячейку**, то есть визуально не различались вообще
  - Все 14 камешков переведены на собственную честную плитку в новой
    строке атласа с цветом настоящего минерала
  - Добавлены 6 руд, которых не было: барит, вольфрамит, флюорит,
    фосфорит, сильвин (калийная соль), уранинит — с жилами под землёй
    и камешками на поверхности. Свинец отделён от серебра: галенит
    раньше ошибочно ронял только серебро, хотя химически это сульфид
    свинца — теперь галенит → свинец, самородное серебро → серебро
  - Таблица руд `HostOreTable` была на фиксированном размере 4 записи
    на породу (`ORE_COUNT`) — при добавлении новых руд это привело бы
    к чтению мимо массива. Переведено на `{указатель, количество}`
    вместо жёсткой константы
  - Новые руды размещены по геологически правдоподобным средам: барит
    и флюорит — карбонатные жилы, фосфорит и калийная соль — морские
    осадки, вольфрам — граниты (грейзены), уран — магматические
    приконтактовые зоны
  - `tools/orecheck.cpp`: все 6 новых жил достижимы под землёй (от
    6700 до 46000 блоков в развёртке), все 7 новых камешков достижимы
    на поверхности, **все 21 тип камешков** проверены на уникальность
    и непрозрачность плитки — 0 нарушений. `geocheck`, `biomecheck`,
    `placecheck`, `mouthcheck` без регрессий

- 🎯 **ВЕЧНЫЕ 0% НА ЗАГРУЗКЕ — НАЙДЕНО И ИСПРАВЛЕНО** (07.08.2026).
  Диагностическая строка `[load]` в консоли ответила за один прогон,
  вместо третьего круга догадок:

  ```
  Spawn: Forest at -96, 2408
  [load] player -96,68,2408 -> chunk -6,150 | under foot: MISSING | ready 0/9
  [gen]  idle   chunks 361 | live 361 | queue m=0
  ```

  - Поиск умеренного биома находил спавн и задавал `player.position`,
    но **`player.camera.position` после него не обновлялась**. А
    `UpdateChunks` центрируется именно по камере — поэтому 361 чанк
    исправно грузился вокруг старой точки камеры, а игрок стоял в
    чанке (-6, 150), где не было ни одного
  - Экран загрузки ждал чанк под ногами игрока. Он не появлялся никогда,
    сколько бы мир ни грузился — отсюда вечные 0% при полностью
    загруженном и уже успокоившемся мире
  - Камера синхронизируется с игроком перед первой `UpdateChunks()`,
    сразу для обеих веток: и для нового мира, и для загруженного
  - Урок: две системы координат на одно и то же место — игрок и камера.
    Любое место, где спавн двигает одну и не двигает другую, ломает
    загрузку молча

- ⏩ **ЗАГРУЗКА: ЧЕСТНАЯ ПОЛОСА И ПРИОРИТЕТ БЛИЖНИХ ЧАНКОВ** (07.08.2026).
  Полоса висела на 0% всю загрузку и прыгала сразу в мир — не ошибка
  отображения, а честный подсчёт: она считала только чанки, уже дошедшие
  до состояния 3, а пока они генерируются, таких нет ни одного:
  - Прогресс теперь складывается из **стадий** каждого чанка (пустой →
    рельеф → меш → живой), поэтому полоса растёт плавно с первых секунд
  - Ожидание сокращено с 25 чанков вокруг игрока (радиус 2) до **9**
    (радиус 1) — в мир пускает почти втрое раньше
  - Очередь мешей теперь **сортируется по расстоянию до игрока**: раньше
    чанки под ногами стояли в общем хвосте за сотнями дальних

- 🧩 **ОТЧЁТ О ПАДЕНИИ ТЕПЕРЬ СО СТЕКОМ** (07.08.2026). Пришёл
  `CRASH_REPORT.txt` с одним адресом `2306354165407` и без единого имени
  функции — по такому диагноз не ставится:
  - `src/Core/CrashTrace.cpp` собирает стек через `StackWalk64` и
    переводит адреса в имена функций с файлом и строкой
  - В Release включены отладочные символы (`/Zi` + `/DEBUG`), иначе
    стек остаётся набором адресов. `/OPT:REF` и `/OPT:ICF` возвращают
    оптимизации, которые `/DEBUG` иначе выключает — скорость не падает
  - `src/main.cpp` в CP1251, правка внесена с сохранением кодировки,
    основной код вынесен в отдельный ASCII-файл
  - ⚠️ Само падение **не найдено**: по одному адресу воспроизвести
    нечего. Следующий отчёт придёт со стеком, тогда и чиним

- 📜 **ЭПОХА I СПРОЕКТИРОВАНА ЦЕЛИКОМ** (07.08.2026) — `ЭПОХА_1.md`.
  Путь от камешка под ногами до литья меди: подбираемые камешки, кремень
  и глина; обтёсывание на сетке 5×5 в пять форм; лепка глины слоями;
  сетка крафта 2×2 в инвентаре с рецептами; яма, костёр, тигель, щипцы,
  форма. Все 16 руд из таблицы запасов с реальным содержанием в жиле,
  плюс сталактиты, сталагмиты и друзы. Код ещё не написан — это проект

- 🐛 **ПУБЛИКАЦИЯ ОБНОВЛЕНИЙ: НАЙДЕНА ПОРЧА КИРИЛЛИЦЫ, ДВЕ ЗАПИСИ
  ВЫПАДАЛИ ИЗ АВТОПАРСЕРА** (07.08.2026). Первая же настоящая публикация
  (`v0.8.0`) ушла на сайт с испорченным заголовком — `«Мир, свет и руды»`
  превратился в символы `�`. Причина: кириллица, переданная как
  `--title` через инструмент Bash в этой среде, портится при передаче в
  argv `python.exe` — сам HTTP-слой и сервер тут ни при чём, проверено
  отдельным прогоном в один процесс без разницы. Данные не терялись бы,
  если бы не аргумент командной строки: заголовок по умолчанию и так
  берётся из ROADMAP.md (файл, а не argv) — вывод на будущее: никогда не
  передавать кириллицу через `--title`/`--summary`, доверять умолчанию
  - Испорченная запись удалена из базы, переопубликована без `--title`
  - Заодно нашлось **два реальных пропуска** в собственном парсере
    ROADMAP: заголовки «Все руды из таблицы запасов» и «Измерены свет и
    мешинг» были написаны с датой на следующей строке, а не на одной с
    `**заголовком**` — формат, который требует `ENTRY_RE`. Обе записи о
    крупной работе этой сессии молча никогда не попадали бы в чейнджлог.
    Переписаны в одну строку, `publish_update.py` подтвердил находку
    (было 22 записи → стало 25, 63 строки → 68)

- 🔌 **API вынесен в отдельный процесс** (07.08.2026) — вики биомов стала кликабельной, три отдельные жалобы решены одним заходом:

  **Обновления не заполнялись сами.** Механизм для этого уже существовал
  (`tools/publish_update.py` парсит ROADMAP.md и постит на `/api/admin/updates`),
  но ни разу не запускался. Нашёл и починил реальный краш инструмента —
  Windows-консоль в кодировке cp1251 падала на кириллице и «→» при выводе;
  добавлен `sys.stdout.reconfigure(encoding="utf-8")`. Backfill опубликован:
  63 строки за 22 записи роадмапа

  **Вики не показывала ничего про биомы, кроме картинки.** Написан
  `tools/biomedump.cpp` — сэмплирует реальный генератор (не парсит его
  логику регексом, она слишком переплетена с рельефом и `variant`-хешем
  для честного разбора) по 8.9 млн колонок и считает по каждому биому:
  температуру, влажность, высоту, поверхностный блок и растительность.
  Пойманы и исправлены две реальные ошибки в первом черновике: температура
  печаталась как 0..1 без единиц вместо `lerp(-30, 38, t)`, которым реально
  пользуется `GetClimate()`; и вода в кроне давала проценты **выше 100%**,
  забивая список растительности на подтопленных колонках. Карточки биомов
  в `/wiki` стали кликабельными — раскрывают климат/грунт/растительность,
  без сторонних библиотек. Заодно поймал свою же ошибку до показа
  пользователю: фильтр `tojson` отдавал HTML-сущности `&#34;` вместо кавычек
  внутри `<script>` — сломанный JS. Причина — Jinja экранирует JSON-строку
  второй раз; фильтр обёрнут в `Markup()`

  **API вынесен в свой процесс на порт 2976** (`api.py`), сайт остался на
  8001 (`app.py`). `/api/updates*`, `/api/wiki*`, `/api/releases*`,
  `/api/admin/*` переехали; `/api/auth/*` и `/api/signup` остались на сайте —
  они завязаны на cookie той же страницы, а лаунчер как нативное приложение
  не подчиняется CORS и может звать любой порт напрямую. Общий
  `apicommon.py`: одна на оба процесса точка доступа к БД (SQLite в режиме
  WAL — иначе два процесса на одном файле дерутся за блокировку),
  константное по времени сравнение `ADMIN_TOKEN` (`hmac.compare_digest`
  вместо `!=`, иначе токен можно подобрать по времени ответа), ограничение
  тела запроса 64 КБ, per-IP лимит на все ручки API (не только
  логин/регистрацию, как было раньше), security-заголовки
  (`X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`), белый
  список CORS-доменов вместо `*`. Всё проверено вживую: оба процесса подняты
  параллельно, весь цикл регистрация → вход → `/me` → выход отработал,
  rate limit на регистрации отсёк лишние попытки (8 прошло, дальше 429),
  публикация обновления через новый порт с очисткой тестовой записи

- 🌊 **ОКЕАН БЕЗ ПОВЕРХНОСТИ — НАЙДЕНА ПРИЧИНА** (07.08.2026). Жалоба
  «часть текстур на океане после прогруза отсутствует» подтвердилась
  замером, а не на глаз:
  - `maxY` чанка считался как `max(высота рельефа, водопад) + 30`, и
    **уровень воды в него не входил**. В глубоком океане дно лежит на
    y≈25, значит `maxY = 55`, а вода стоит до y=60 — мешер обрывался
    на пять блоков ниже поверхности. Верх океана генерировался и
    никогда не рисовался, поэтому было видно тёмное ступенчатое дно
  - `tools/maxycheck.cpp` проверяет инвариант «выше `maxY` не может
    быть ни одного непустого блока». По старой формуле пряталось
    **157 440 блоков, все до единого — вода**, в **136 чанках из 729**
    (каждый пятый). После правки — **0**
  - Правка: в `maxY` теперь входят уровень моря для затопленных колонок
    и уровень воды реки

- ✨ **ТЕНИ ЗАРАБОТАЛИ, ОСВЕЩЕНИЕ В ДУХЕ BSL** (07.08.2026). Оказалось,
  что движок **каждый кадр рисовал карту теней 2048×2048 и выбрасывал
  её**: `RenderShadowPass` считал матрицу и заполнял буфер, но `fog.fs`
  не объявлял ни `shadowMap`, ни `shadowMVP`, а текстура не привязывалась
  к шейдеру. Работа оплачивалась, результат не использовался:
  - Тени подключены с фильтрацией PCF 3×3 и наклонным смещением, чтобы
    не было полос самозатенения
  - Освещение переписано: полусферический ambient (небо сверху, отражение
    от земли снизу), тёплое солнце с покраснением у горизонта, слабый
    отражённый свет с теневой стороны
  - Плёночный тонемаппинг ACES и лёгкая подкраска вместо обрезки цвета
  - `shadowStrength` гаснет к закату и обнуляется, когда прохода теней
    не было, — иначе мир ушёл бы в черноту

- 🌫️ **ПЛАВНЫЕ ПЕРЕХОДЫ МЕЖДУ БИОМАМИ** (07.08.2026). Границы рвались
  прямыми линиями, потому что размывались только температура и влажность,
  а остальные три признака выбора биома — нет:
  - `variant` был хешем по клетке 128×128 блоков — отсюда прямоугольные
    швы на карте. Заменён на плавный шум с джиттером
  - `relief` и порог низины `heightBase` брались без размытия — добавлен
    джиттер, как у температуры
  - Подварианты гор (`peak`, `vent`) тоже сидели на блочных хешах — тоже
    переведены на шум
  - `tools/blendcheck.cpp`: из **512 337** пересечённых границ резким
    остаётся **5.5%**, разнообразие не пострадало — 35 биомов в полосе
    12000×12000. `biomecheck`, `mouthcheck`, `placecheck` без регрессий

- ⚡ **ЗАГРУЗКА МИРА: снята квадратичная сложность** (04.08.2026 06:00).
  Жалоба «мир очень долго генерируется» подтвердилась, но причина оказалась
  не в генерации:
  - Написан `tools/genbench.cpp` — разбивка времени по этапам. **Генерация
    1.47 мс на чанк**: при радиусе 17 это 1225 чанков и всего **1.8 с**.
    Твёрдые блоки 57%, `GetColumnInfo` 21%, высота 12%, порода 7%,
    биом 2%, растительность 1%
  - **Настоящая причина**: `UpdateChunks` для каждой позиции-кандидата
    линейно перебирал **все** загруженные чанки — до **1.5 млн сравнений
    каждый кадр** при радиусе 17, и так постоянно, а не только при загрузке.
    Заменено на хеш-индекс по координатам: O(n²) → **O(n)**
  - **Потоки распределялись жёстко**: 2 на рельеф независимо от процессора,
    меш-потоки простаивали в ожидании первой стадии конвейера. Стало
    пропорционально ядрам — на 12 ядрах 4 на рельеф, 7 на меши; в лог
    пишется фактическое распределение
- 📊 **Измерены свет и мешинг, найдена ошибка в прошлом замере** (07.08.2026).
  Написан `tools/loadbench.cpp` — он прогоняет полный путь
  чанка (рельеф → свет → меш) на настоящем `Chunk`, а не на одном
  генераторе:

  | Этап | мс на чанк | доля |
  |------|-----------|------|
  | `GenerateTerrain` | 7.31 | 70% |
  | `BuildMeshCPU` | 2.02 | 19% |
  | освещение | 1.09 | 10% |
  | **итого** | **10.4** | |

  - **Прошлая цифра 1.47 мс на чанк была занижена.** `genbench` мерил
    чужую область карты и заполнял колонку только до y=120, тогда как
    `GenerateTerrain` проходит все 256 уровней. Настоящая стоимость
    рельефа около спавна — **7.3 мс**, в пять раз больше. Вывод «генерация
    не узкое место» был неверен: она и есть 70% времени
  - **Пропуск пустого неба**: `GetBlockFast` вызывался для всех 256
    уровней, хотя выше поверхности заведомо воздух. Потолок колонки
    считается точно — `max(высота + 24, уровень моря, водопад, уровень
    реки)`, где 24 это предельная высота кроны дерева. **66.6% колонки
    пропускается**, рельеф 7.93 → 7.31 мс. `tools/ceilcheck.cpp`
    сверил 1 440 000 блоков полного и укороченного заполнения:
    **0 расхождений**, старые сиды сохраняются
  - **Проверена и отвергнута гипотеза о кэше.** Запись шла с шагом 256
    байт (индекс `x + 16*(z + 16*y)`, внутренний цикл по y). Переписал
    обход на последовательный с подсчётом колонок заранее — **7.46 мс,
    то есть ничего**. Откатил: сложность без выигрыша не остаётся в коде.
    Стоимость сидит внутри самого `GetBlockFast`, а не в памяти
  - ⚠️ Осталось неизмеренным: заливка мешей на видеокарту (лимит 200
    загрузок на кадр) — требует контекста OpenGL, в консольный бенчмарк
    не выносится

#### ⬜ Осталось:
- 💧 **Текучесть воды и размыв берегов** — вода должна течь по-настоящему,
  а река со временем **размывать блоки** и менять русло, как в жизни:
  подмывать внешний берег излучины, наносить отмель на внутреннем,
  углублять плёсы, сносить рыхлый грунт быстрее камня. Сейчас русло
  статично: оно вырезано в генерации и не меняется. Требует симуляции
  жидкости с расходом и переносом наносов — крупная задача, ставить после
  секций чанка (см. [ОПТИМИЗАЦИЯ.md](ОПТИМИЗАЦИЯ.md)), иначе пересчёт
  целых чанков на каждый сдвиг русла убьёт производительность
- 🖼️ **Графика уровня BSL**: каскадные тени, объёмный свет, отражения на воде,
  SSAO, цветокоррекция и блум. В движке уже есть `shadowShader` и `shadowMap` —
  это точка входа
- 🧩 **Пользовательские шейдеры**: папка `shaders/<пак>/`, движок подхватывает
  `fog.vs/fs`, `water.vs/fs`, `shadow.vs/fs` из выбранного пака, uniform-контракт
  фиксируется и документируется в вики (`/wiki` уже генерируется из исходников —
  добавить туда раздел с таблицей uniform-ов) + отдельный гайд в вики «как
  сделать свой шейдер-пак» с примером на базе текущего `fog.fs`
- 🌊 **Брызги водопадов** (частицы) — ждёт систему частиц
- 🌸 **Растительность v2**: кувшинки ~~ягодные кусты~~ ✅
- 🏔️ **Овраги и каньоны**: разломы 10-20 блоков
- 🏖️ **Пляжи v2**: ракушки, галька, плавник
- 🪜 **Строй-блоки**: ступеньки, заборы (нужна не-кубическая геометрия)
- 🌑 **Тени от деревьев** (shadow map), затем объёмные god rays
- 🍳 **Готовка на костре** — сырое/жареное, порча еды со временем

---

## ⬜ ФАЗА 2: ФУНДАМЕНТ ВЫЖИВАНИЯ (v0.8 – v0.9.5)

> Полный дизайн эпох — в [ПРОГРЕССИЯ.md](ПРОГРЕССИЯ.md)

### ⬜ v0.8 «Вода»
- Жидкостная симуляция (spreading cellular automata)
- Уровни воды 1-8, течение, бесконечные источники
- Плавание игрока
- Лёд: замерзание/таяние
- Лава: spreading, поджигание, обсидиан при контакте с водой

### ⬜ v0.9 🪨 **Эпоха I — Первичный Хаос**
- **Кнаппинг**: интерфейс скалывания 5×5, ошибка портит заготовку
- **Костёр**: розжиг трением/искрой, сухое топливо, гаснет под дождём
- **Физика деревьев**: сруб основания валит весь ствол с кроной
- **Питание и порча**: срок годности, отравление ягодами и грибами
- **Копьё и лук**, шкуры, спальник
- Мобы: пассивные (корова, свинья, овца, курица)
- Здоровье, голод
- Тростниковая корзина — первое хранилище

### ⬜ v0.9.5 🏺 **Эпоха II — Керамика и Литьё**
- **Гончарство**: послойная лепка сосудов, тиглей, форм
- **Ямный обжиг (Pit Kiln)**: яма, солома, дрова, долгое ожидание
- **Литьё меди**: тигель + ручные меха, медь только льётся, не куётся
- **Температурная шкала**: перегрел — сжёг, недогрел — не расплавил
- Первое земледелие: дикие злаки, семена, ручные грядки
- Крафт: верстак, инструменты, печь
- Структуры: деревни, подземелья

---

## ⬜ ФАЗА 3: МЕХАНИЗАЦИЯ (v1.0 – v1.2)

### ⬜ v1.0 ⚙️ **Эпоха III — Кинематика и Сплавы**
- **Бронза**: медь 88-92% + олово 8-12%, ошибка = «грязный металл»
- **Наковальня**: мини-игра ковки «тяни/бей/пробей»
- **Водяное колесо** на реку → валы и шестерни
- **Дробилка руды** (+25% выхода) и **жернова** от привода
- Гужевые телеги, дороги
- Бесконечный мир, сохранения v2, Lua API v2

### ⬜ v1.0.5 «Соседи» — мультиплеер
- TCP/UDP, синхронизация, чат
- ⚠️ Критично для сервера: см. «Прогрессия сервера» в ПРОГРЕССИЯ.md

### ⬜ v1.1 🌋 **Эпоха IV — Термодинамика и Сталь**
- **Сыродутная печь (Bloomery)**: слои руды и угля → крица
- **Проковка крицы**: выбивание шлака → железо → сталь
- **Механизированный поддув**: меха на водяном колесе
- **Геологоразведочный бур**: анализ пород на глубине
- **Обвалы шахт (Cave-in)**: глубина требует деревянных креплений
- Пластинчатая броня по частям

### ⬜ v1.2 🏰 **Эпоха V — Инфраструктура**
- **Порох**: сера + селитра + уголь, взрывная проходка
- **Ветряные мельницы** — от динамической системы ветра
- **Плуг с лошадью**: вспашка полосой
- **Книги-чертежи**: знание становится товаром
- Мосты на противовесах, шахтные лифты, подъёмные решётки
- Деревни NPC, торговля, репутация

---

## ⬜ ФАЗА 4: ИНДУСТРИЯ (v1.3 – v1.5)

### ⬜ v1.3 🏭 **Эпоха VI — Паровой Двигатель**
- **Паровой котёл**: давление, предохранительный клапан, риск взрыва
- **Паровая машина**: мощность в любую точку через трубы
- **Конвейеры и сортировщики**
- **Карьерный бур**: добыча без игрока
- Насосы, железная дорога
- 🎯 Квест эпохи: замкнутый цикл «Бур → Конвейер → Печь → Склад»

### ⬜ v1.4 ⚡ **Эпоха VII — Магистрали EIDOS**
- **ГЭС на водопадах**, паровые турбины, ветрогенераторы
- **Электросети**: провода, трансформаторы, потери от расстояния
- **Электролиз**: алюминий, чистый кремний, +50% выхода
- **Релейная логика**: И/ИЛИ/НЕ, датчики, авто-остановка

### ⬜ v1.5 💻 **Эпоха VIII — Информационная**
- **Микроконтроллеры на Lua** (язык уже в движке)
- **Роботизированные манипуляторы**
- **Дроны-курьеры**, автокрафт
- **Гео-сканер**: 3D-карта жил — геология Эпохи I замыкает круг
- Ядерный реактор под управлением скрипта

---

## ⬜ ФАЗА 5: ЭКСПАНСИЯ (v2.0 – v2.5)

### ⬜ v2.0 🚀 **Эпоха IX — Орбитальный Вектор**
- Многоступенчатые ракеты, топливо из электролиза
- Спутники: картография планеты, глобальная телеметрия
- Другие планеты и луны: своя геология, гравитация, кислород как ресурс
- Автономные посадочные модули, строящие базу по программе

### ⬜ v2.5 🧬 **Эпоха X — Абсолют EIDOS**
- Нанофабрикация, квантовые порталы
- **ИИ-Ядро**: приказы естественным языком
- Глобальный терраформинг
- 🏆 Финал: ИИ строит монумент Первому Кремнёвому Ножу игрока

---

## 🏛️ ЭПОХИ EIDOS (сквозная система)

> Философия, механики и цепочки квестов каждой эпохи — в [ПРОГРЕССИЯ.md](ПРОГРЕССИЯ.md)

| # | Эпоха | Ключевой материал | Энергия | Что автоматизируется | Версия |
|---|-------|-------------------|---------|----------------------|:------:|
| I | 🪨 Первичный Хаос | Кремень, дерево | Огонь | ничего | v0.9 |
| II | 🏺 Керамика и Литьё | Медь, глина | Уголь | обжиг, рост растений | v0.9.5 |
| III | ⚙️ Кинематика | Бронза (Cu+Sn) | Вода, ветер | дробление, помол | v1.0 |
| IV | 🌋 Термодинамика | Железо, сталь | Уголь | поддув, жар | v1.1 |
| V | 🏰 Инфраструктура | Сталь, золото | Порох, тягло | вспашка, логистика | v1.2 |
| VI | 🏭 Паровой Двигатель | Сталь, латунь | Пар | добыча, сортировка, плавка | v1.3 |
| VII | ⚡ Магистрали EIDOS | Медь, кремний | Электричество | энергоснабжение, авто-стоп | v1.4 |
| VIII | 💻 Информационная | Кремний, пластик | Ядерная | автокрафт, дроны, реакторы | v1.5 |
| IX | 🚀 Орбитальный Вектор | Титан, композиты | Ракетное | развёртывание аванпостов | v2.0 |
| X | 🧬 Абсолют EIDOS | Наноматериалы | Квантовая | всё, включая экспансию | v2.5 |

**Стержень**: «От первичной материи к абсолютному контролю». Каждая эпоха освобождает от ручного труда предыдущей. Механика не исчезает — она автоматизируется, рецепт остаётся тем же.

---

## 📊 ДЕТАЛЬНЫЙ СТАТУС ПО ВЕРСИЯМ

| Версия | Эпоха | Статус | Фичи | Дата |
|--------|-------|:------:|:----:|------|
| v0.1–0.5 | — | ✅ | 5 | июль 2026 |
| v0.6 | — | ✅ | 7 | июль 2026 |
| v0.6.1 | — | ✅ | 12 (18/25 багов) | 26.07.2026 |
| v0.7 | — | 🟡 | 6/8 | 02.08.2026 |
| ↳ Реки v2 | — | ✅ | переделка | 01.08.2026 |
| ↳ Древо квестов | — | ✅ | Эпоха I играбельна | 01.08.2026 |
| ↳ Текстуры v3 | — | ✅ | взрослая палитра | 02.08.2026 |
| ↳ Факелы + блочный свет | 🪨 I | ✅ | пещеры играбельны | 02.08.2026 |
| ↳ Температура + вкладки | 🪨 I | ✅ | климат в °C | 02.08.2026 |
| ↳ Режим выживания | 🪨 I | ✅ | HP/еда/вода/стамина | 02.08.2026 |
| v0.8 | — | ⬜ | 0/5 | — |
| v0.9 | 🪨 I | ⬜ | 0/8 | — |
| v0.9.5 | 🏺 II | ⬜ | 0/7 | — |
| v1.0 | ⚙️ III | ⬜ | 0/6 | — |
| v1.0.5 | — | ⬜ | мультиплеер | — |
| v1.1 | 🌋 IV | ⬜ | 0/6 | — |
| v1.2 | 🏰 V | ⬜ | 0/6 | — |
| v1.3 | 🏭 VI | ⬜ | 0/6 | — |
| v1.4 | ⚡ VII | ⬜ | 0/4 | — |
| v1.5 | 💻 VIII | ⬜ | 0/5 | — |
| v2.0 | 🚀 IX | ⬜ | 0/4 | — |
| v2.5 | 🧬 X | ⬜ | 0/4 | — |

---

## 🎯 БЛИЖАЙШИЕ ЗАДАЧИ (v0.7 — доделать)

1. 🌸 Растительность v2: ~~папоротники, камыши, ягодные кусты~~ ✅ → кувшинки
2. 🏔️ Овраги и каньоны
3. 🏖️ Пляжи v2: ракушки, галька, плавник
4. ~~🔥 Факелы с динамическим светом~~ ✅
5. 🪜 Строй-блоки: ~~доски~~ ✅ → ступеньки, заборы (нужна не-кубическая геометрия)
6. 🌑 Тени от деревьев (shadow map), затем объёмные god rays
7. 🍳 Костёр и готовка — сырое/жареное, порча еды
8. ⛏️ Кнаппинг из кремня (Эпоха I) — кремень уже добывается из галечника
9. 📊 Счётчики F3 и бенчмарк — без замеров оптимизацию не начинать
   (см. ОПТИМИЗАЦИЯ.md, часть III)

---

*Обновлено: 09.08.2026 05:15*
